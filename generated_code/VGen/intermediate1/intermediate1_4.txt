// Declare the inputs and outputs
input a, b;
output cout, sum;

// Implement the half adder logic
assign sum = a ^ b;
assign cout = a & b;

endmodule

// Note: This module implements a half adder which adds two bits 'a' and 'b' and produces a sum and carry-out.