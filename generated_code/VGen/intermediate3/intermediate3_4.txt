// Define the Galois LFSR feedback polynomial coefficients
parameter POLY = 5'b10101;

// Implement the LFSR logic
assign feedback_value = r_reg[4] ^ r_reg[2]; // Feedback taps at positions 5 and 3
assign r_next[0] = feedback_value;
assign r_next[1:4] = r_reg[0:3];

always @(posedge clk or posedge reset) begin
    if (reset) begin
        r_reg <= 5'b00001; // Initial seed value
    end else begin
        r_reg <= r_next;
    end
end

// Output the LFSR register value
assign q = r_reg;

endmodule

// To test the module, instantiate it in a testbench and apply clock and reset signals along with observing the output q.